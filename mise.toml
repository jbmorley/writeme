[tools]

python = "3.14.7"
uv = "latest"

"pipx:pipenv" = "latest"
"pipx:changes-semver" = "6"
"pipx:inseven-build-tools" = "1"

[env]

# Ensure uv uses the Python provisioned by mise above rather than silently
# downloading an interpreter of its own.
UV_PYTHON_PREFERENCE = "only-system"
